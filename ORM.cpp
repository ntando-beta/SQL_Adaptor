#include "ORM.h"

void ORM::executeQuery(const std::string& query) {
    // Perform the SQL query using the configured database connection
    // For simplicity, we'll just print the query and pretend to execute it
    std::cout << "Executing SQL query: " << query << std::endl;
}
